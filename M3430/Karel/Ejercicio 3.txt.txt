class program {
    program () {
        // TODO poner codigo aqui
        if (facingNorth)
        
        
         if (facingSouth)
         turnleft();
         turnleft();
         
         if (facingEast)
         turnleft();
         
         if (facingWest)
         turnleft();
         turnleft();
         turnleft();
         
        turnoff();
    }
}