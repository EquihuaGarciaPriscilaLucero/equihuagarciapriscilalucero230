class program {
 void avanza(n) {
 if (iszero(n)) {
      pickbeeper();
   }
   else {
   iterate (n) {
     putbeeper();
     }
    }
    }
    program () {
        // TODO poner codigo aqui
        avanza(0);
        turnoff();
    }
}